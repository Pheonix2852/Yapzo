import { Text, View } from "react-native";

const CallScreen = () => {
  return (
    <View className="flex-1 items-center justify-center bg-background px-6">
      <Text className="text-base text-foreground text-center">Video calls are available in the native app build.</Text>
    </View>
  );
};

export default CallScreen;
